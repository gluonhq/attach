dependencies {
    implementation project(':util')
}

ext.moduleName = 'com.gluonhq.attach.runtimeargs'
ext.description = 'Common API to for injecting runtime arguments into Attach applications, ' +
        'and listening for these arguments.'